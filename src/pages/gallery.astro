---
import Layout from '../layouts/Layout.astro';
---

<Layout title="Gallery" description="Take a virtual tour of The Bridge Chiropractic in San Diego. See our calm, welcoming clinic space and treatment rooms.">
  <!-- Hero Section -->
  <section class="hero-fullbleed">
    <video
      class="hero-fullbleed__image"
      autoplay
      muted
      loop
      playsinline
    >
      <source src="https://res.cloudinary.com/dku1gnuat/video/upload/v1772135510/A_0004C206H251212_163500CS_CANON_puvlaw.mp4" type="video/mp4" />
    </video>
    <div class="hero-fullbleed__overlay"></div>
    <div class="hero-fullbleed__content">
      <div class="container">
        <span class="section__eyebrow">Our Space</span>
        <h1 class="hero-fullbleed__title">Photo Gallery</h1>
        <p class="hero-fullbleed__subtitle">Take a look at our calm, welcoming clinic environment.</p>
      </div>
    </div>
  </section>

  <!-- Gallery Intro -->
  <section class="section">
    <div class="container">
      <div class="gallery-intro reveal">
        <p>At The Bridge Chiropractic, we've created a space that reflects our approach to care: calm, comfortable, and focused on your wellbeing. Here's a glimpse of what you'll experience when you visit us.</p>
      </div>

      <!-- Gallery Grid -->
      <div class="gallery-grid">
        <div class="gallery-item gallery-item--large reveal">
          <img src="https://images.unsplash.com/photo-1519494026892-80bbd2d6fd0d?auto=format&fit=crop&w=1200&q=80" alt="The Bridge Chiropractic clinic exterior" />
          <div class="gallery-item__overlay">
            <span>Our Clinic</span>
          </div>
        </div>

        <div class="gallery-item reveal">
          <img src="https://images.unsplash.com/photo-1629909613654-28e377c37b09?auto=format&fit=crop&w=800&q=80" alt="Welcoming reception area" />
          <div class="gallery-item__overlay">
            <span>Reception Area</span>
          </div>
        </div>

        <div class="gallery-item reveal">
          <img src="https://images.unsplash.com/photo-1519494140681-8f17d830d3c9?auto=format&fit=crop&w=800&q=80" alt="Treatment room" />
          <div class="gallery-item__overlay">
            <span>Treatment Room</span>
          </div>
        </div>

        <div class="gallery-item reveal">
          <img src="https://images.unsplash.com/photo-1576091160550-2173dba999ef?auto=format&fit=crop&w=800&q=80" alt="Chiropractic adjustment table" />
          <div class="gallery-item__overlay">
            <span>Adjustment Table</span>
          </div>
        </div>

        <div class="gallery-item reveal">
          <img src="https://images.unsplash.com/photo-1571019613454-1cb2f99b2d8b?auto=format&fit=crop&w=800&q=80" alt="Movement and exercise area" />
          <div class="gallery-item__overlay">
            <span>Exercise Area</span>
          </div>
        </div>

        <div class="gallery-item gallery-item--large reveal">
          <img src="/images/IMG_0954.JPG" alt="Dr. Nathan Beyerl" />
          <div class="gallery-item__overlay">
            <span>Dr. Nathan Beyerl</span>
          </div>
        </div>

        <div class="gallery-item reveal">
          <img src="https://images.unsplash.com/photo-1578662996442-48f60103fc96?auto=format&fit=crop&w=800&q=80" alt="Comfortable waiting area" />
          <div class="gallery-item__overlay">
            <span>Waiting Area</span>
          </div>
        </div>

        <div class="gallery-item reveal">
          <img src="https://images.unsplash.com/photo-1576091160399-112ba8d25d1d?auto=format&fit=crop&w=800&q=80" alt="Patient consultation" />
          <div class="gallery-item__overlay">
            <span>Consultation</span>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Note Section -->
  <section class="section section--gray reveal">
    <div class="container">
      <div class="gallery-note glass-card">
        <h2>A Space Designed for Healing</h2>
        <p>Our clinic was designed with your comfort in mind. From the moment you walk in, you'll notice the difference—a calm, unhurried environment where healing begins. No crowded waiting rooms, no rushed appointments. Just focused, personalized care in a space that supports your wellbeing.</p>
        <a href="/contact" class="btn btn--primary btn--lg">Schedule Your Visit</a>
      </div>
    </div>
  </section>

  <!-- Social Section -->
  <section class="section reveal">
    <div class="container">
      <div class="social-cta glass-card">
        <div class="social-cta__content">
          <h2>Follow Us on Social Media</h2>
          <p>Stay connected and see more of what we do at The Bridge Chiropractic.</p>
        </div>
        <div class="social-cta__links">
          <a href="https://instagram.com/bridgechiropracticsd" target="_blank" rel="noopener" class="social-link">
            <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="2" y="2" width="20" height="20" rx="5" ry="5"></rect><path d="M16 11.37A4 4 0 1 1 12.63 8 4 4 0 0 1 16 11.37z"></path><line x1="17.5" y1="6.5" x2="17.51" y2="6.5"></line></svg>
            <span>@bridgechiropracticsd</span>
          </a>
          <a href="https://facebook.com/bridgechiropracticsd" target="_blank" rel="noopener" class="social-link">
            <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M18 2h-3a5 5 0 0 0-5 5v3H7v4h3v8h4v-8h3l1-4h-4V7a1 1 0 0 1 1-1h3z"></path></svg>
            <span>bridgechiropracticsd</span>
          </a>
        </div>
      </div>
    </div>
  </section>
</Layout>

<style>
  /* Gallery Intro */
  .gallery-intro {
    max-width: 800px;
    margin: 0 auto var(--space-4xl);
    text-align: center;
  }

  .gallery-intro p {
    font-size: 1.25rem;
    line-height: 1.8;
    color: var(--color-gray-dark);
  }

  /* Gallery Grid */
  .gallery-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: var(--space-lg);
  }

  .gallery-item {
    position: relative;
    overflow: hidden;
    border-radius: var(--radius-lg);
    aspect-ratio: 1;
    background: var(--color-gray-light);
    box-shadow: var(--shadow-md);
    transition: all var(--transition-normal);
  }

  .gallery-item:hover {
    transform: translateY(-4px);
    box-shadow: var(--shadow-lg);
  }

  .gallery-item--large {
    grid-column: span 2;
    aspect-ratio: 2/1;
  }

  .gallery-item img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: transform var(--transition-slow);
  }

  .gallery-item:hover img {
    transform: scale(1.08);
  }

  .gallery-item__overlay {
    position: absolute;
    bottom: 0;
    left: 0;
    right: 0;
    padding: var(--space-xl);
    background: linear-gradient(to top, rgba(0, 0, 0, 0.8), transparent);
    opacity: 0;
    transition: opacity var(--transition-normal);
  }

  .gallery-item:hover .gallery-item__overlay {
    opacity: 1;
  }

  .gallery-item__overlay span {
    color: var(--color-white);
    font-size: 1.125rem;
    font-weight: 600;
    font-family: var(--font-heading);
  }

  /* Gallery Note */
  .gallery-note {
    text-align: center;
    max-width: 800px;
    margin: 0 auto;
    padding: var(--space-4xl);
  }

  .gallery-note h2 {
    font-family: var(--font-heading);
    font-size: 2.5rem;
    color: var(--color-primary);
    margin-bottom: var(--space-lg);
  }

  .gallery-note p {
    font-size: 1.125rem;
    line-height: 1.8;
    color: var(--color-gray-dark);
    margin-bottom: var(--space-2xl);
  }

  /* Social CTA */
  .social-cta {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: var(--space-2xl);
    padding: var(--space-3xl);
  }

  .social-cta__content h2 {
    font-family: var(--font-heading);
    font-size: 2rem;
    color: var(--color-primary);
    margin-bottom: var(--space-md);
  }

  .social-cta__content p {
    margin: 0;
    color: var(--color-gray-dark);
    font-size: 1.125rem;
  }

  .social-cta__links {
    display: flex;
    gap: var(--space-lg);
    flex-shrink: 0;
  }

  .social-link {
    display: flex;
    align-items: center;
    gap: var(--space-md);
    padding: var(--space-lg) var(--space-xl);
    background: rgba(255, 255, 255, 0.8);
    backdrop-filter: blur(8px);
    border-radius: var(--radius-md);
    color: var(--color-charcoal);
    text-decoration: none;
    font-weight: 600;
    transition: all var(--transition-fast);
    border: 1px solid rgba(0, 0, 0, 0.08);
  }

  .social-link:hover {
    background: var(--color-accent);
    color: var(--color-white);
    border-color: var(--color-accent);
    transform: translateY(-2px);
    box-shadow: var(--shadow-md);
  }

  .social-link svg {
    flex-shrink: 0;
  }

  /* Responsive */
  @media (max-width: 1024px) {
    .gallery-grid {
      grid-template-columns: repeat(2, 1fr);
    }

    .social-cta {
      flex-direction: column;
      text-align: center;
    }

    .social-cta__links {
      width: 100%;
      justify-content: center;
    }
  }

  @media (max-width: 768px) {
    .gallery-intro p {
      font-size: 1.125rem;
    }

    .gallery-grid {
      grid-template-columns: 1fr;
      gap: var(--space-md);
    }

    .gallery-item--large {
      grid-column: span 1;
      aspect-ratio: 16/9;
    }

    .gallery-item {
      aspect-ratio: 4/3;
    }

    .gallery-note {
      padding: var(--space-3xl);
    }

    .gallery-note h2 {
      font-size: 2rem;
    }

    .social-cta {
      padding: var(--space-2xl);
    }

    .social-cta__content h2 {
      font-size: 1.75rem;
    }

    .social-cta__links {
      flex-direction: column;
    }

    .social-link {
      justify-content: center;
      width: 100%;
    }
  }

  @media (max-width: 480px) {
    .gallery-note {
      padding: var(--space-2xl);
    }

    .gallery-note h2 {
      font-size: 1.75rem;
    }

    .gallery-item__overlay {
      opacity: 1;
      background: linear-gradient(to top, rgba(0, 0, 0, 0.7), transparent);
    }
  }
</style>

<script>
  // Scroll-triggered reveal animations
  const revealElements = document.querySelectorAll('.reveal');

  const revealObserver = new IntersectionObserver((entries) => {
    entries.forEach(entry => {
      if (entry.isIntersecting) {
        entry.target.classList.add('revealed');
        revealObserver.unobserve(entry.target);
      }
    });
  }, {
    threshold: 0.1,
    rootMargin: '0px 0px -50px 0px'
  });

  revealElements.forEach(el => revealObserver.observe(el));
</script>
</Layout>
